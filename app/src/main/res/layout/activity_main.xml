<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>
        <variable
            name="repoViewModel"
            type="com.spacitron.reposlistapp.repoviewmodel.RepositoryViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <android.support.v7.widget.RecyclerView
            bind:items="@{ repoViewModel.repositoriesObservable }"
            bind:onItemSelected="@{ repoViewModel }"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="vertical" />
        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:visibility="@{ repoViewModel.isLoading() ? View.VISIBLE : View.GONE }"
            />
    </RelativeLayout>
</layout>